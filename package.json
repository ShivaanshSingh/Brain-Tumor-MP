{
  "name": "brain-tumor-insight-system",
  "version": "1.0.0",
  "description": "Deep Learning-Based Brain Tumor Insight System",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "npx concurrently \"node --watch backend/server.js\" \"npm run dev --prefix frontend\"",
    "backend": "npm run start --prefix backend",
    "frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "firebase": "^12.12.0"
  }
}
